/**
 * Input for the best score candidate selector.
 * @category Reflective Prompt Evolution
 */
export interface BestScoreCandidateSelectorInput {
  /**
   * Whether to only consider new candidates. Defaults to `false`.
   */
  newCandidatesOnly?: boolean;
}