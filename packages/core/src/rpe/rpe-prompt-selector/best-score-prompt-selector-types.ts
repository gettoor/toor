/**
 * Input for the best score prompt selector.
 * @category Reflective Prompt Evolution
 */
export interface BestScorePromptSelectorInput {
  /**
   * Whether to only consider candidates. Defaults to `false`.
   */
  candidatesOnly?: boolean;
}